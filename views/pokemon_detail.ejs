<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>status</title>
</head>
<body>
  <a href="/pokemon">一覧に戻る</a>
  <a href="/calc/page/cp">個体値計算へ</a>
  <hr>
  <table>
    <p>No.<%= data[0].number %></p>
    <p><%= data[0].name %></p>
    <p>
    <% if(data[0].type != "タイプなし"){ %>
          <%= data[0].type %>
    <% } 
       if(data[1].type != "タイプなし") {%>
        <%= data[1].type %>
    <% } %>
    </p>
      <p>最大CP：<%= cp %></p>
      <tr>
          <td>攻撃：</td>
          <td><%= data[0].attack %></td>
      </tr>
      <tr>
          <td>防御：</td>
          <td><%= data[0].defence %></td>
      </tr>
      <tr>
          <td>HP：</td>
          <td><%= data[0].hp %></td>
      </tr>
    </table>
      
</body>
</html>